# Variáveis de ambiente para o Frontend React (Vite)
# NÃO adicione valores reais neste arquivo. Ele serve apenas de exemplo.

# URL base apontando para o Backend FastAPI
# Se em produção, modifique de localhost para o domínio da sua API.
VITE_API_URL=http://localhost:8000
